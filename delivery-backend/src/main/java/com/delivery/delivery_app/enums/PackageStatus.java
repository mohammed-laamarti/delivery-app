package com.delivery.delivery_app.enums;

public enum PackageStatus {
    TO_CONFIRM,
    TO_RECEIVE,
    AT_AGENCY,
    TO_DELIVER,
    ASSIGNED,
    IN_DELIVERY,
    DELIVERED,
    POSTPONED,
    RETURNED,
    RETURN_SHIPPED,
    CANCELLED
}
